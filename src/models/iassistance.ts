export default interface IAssistance {
    _id ?: string,
    carnet ?: number | string,
    nombre_alumno ?: string,
    nombre_evento ?: string,
    id_evento ?: string,
    captura ?: string,
    fecha ?: string,
    server ?: string,
    __v ?: number,
}