const HOST = 'api.apir2g4.tk'; //'balancer-1447780461.us-east-2.elb.amazonaws.com';
const PORT = ''; //:8000
const BASE_URL = `http://${HOST}${PORT}`;

export const URI_REPORT = `${BASE_URL}/reportes`;
export const URI_ASSIST = `${BASE_URL}/asistencia`;
